/*fn main() {
    let x: i32 = 5;
    let y: i32 = 5;

//assert_eq! takes two parameters and returns a success if both are equal
    assert_eq!(x,y);
    println!("success")
}

*/
// Fill the blanks in the code to make it compile
fn main() {
    let mut x = 1;
    x = x + 2; //3
    
    assert_eq!(x, 3);
    println!("Success!");
}